import React from "react";
import classes from "./style.module.css";

const Footer = () => {
  return (
    <footer className={classes.footerContainer}>
      <div className={classes.footerWrapper}>NendoGo</div>
    </footer>
  );
};

export default Footer;
